<?xml version="1.0" encoding="utf-8"?>
<!-- 更新检查使用网络，APK 下载后交给系统安装器处理。 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 不申请 CAMERA 权限：拍照由系统相机处理；Photo Picker 也无需存储权限。 -->

    <application
        android:name=".ZongceApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zongce"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Zongce">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            桌面小组件：只承载「拍照 / 相册」两个入口，点击后把带 action 的 Intent 交给
            MainActivity（singleTop），由它转发给录入页。组件本身不碰数据库、不保存照片。
            exported=false 沿用 AppWidget 的常规写法：广播由系统发出，无需对外暴露。
        -->
        <receiver
            android:name=".widget.JicunWidgetReceiver"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/jicun_widget_info" />
        </receiver>

        <!-- 拍照临时文件 & 导出 ZIP 分享 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
